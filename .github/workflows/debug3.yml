name: Debug Test 3
on:
  workflow_dispatch:
    inputs:
      count:
        description: how many
        required: true
        default: '5'
      prefix:
        description: name prefix
        required: true
        default: 'zen-proxy'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ok
